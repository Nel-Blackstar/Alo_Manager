package com.live.paie.repository;

import com.live.paie.entities.Prets;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PretsRepository extends JpaRepository<Prets, Long> {
}
