package com.live.paie.repository;

import com.live.paie.entities.CNPS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CNPSRepository extends JpaRepository<CNPS, Long> {
}
