package com.live.paie.repository;

import com.live.paie.entities.ParametreEntreprise;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParametreEntrepriseRepository extends JpaRepository<ParametreEntreprise, Long> {
}
