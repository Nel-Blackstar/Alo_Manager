package com.live.paie.repository;

import com.live.paie.entities.PrimesFixes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrimesFixesRepository extends JpaRepository<PrimesFixes, Long> {
}
